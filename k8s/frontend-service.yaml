apiVersion: v1
kind: Service
metadata:
  name: ebank-node
  namespace: ebank
spec:
  type: ClusterIP
  selector:
    app: ebank-node
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000